import { Token } from "../components/ide/Token";
import type { SceneType } from "../types/Scene";

export default {
  editor: [
    {
      value: (
        <>
          <Token value="# 何者でもない、何者にもなれなかったファイルです" />
        </>
      ),
    },
  ],
} as SceneType;
